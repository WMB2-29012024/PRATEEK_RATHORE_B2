//  Create an object with key “primitivetypes”, and value should be an array
//  of strings containing names of all primitive data types in JS
// Create an object with key "primitivetypes" and an array of primitive data types
let typesObject = {
    primitivetypes: ["string", "number", "boolean",
     "null", "undefined", "bigint", "symbol"]
};

// Log the object
console.log("Object with primitive types:", typesObject);
