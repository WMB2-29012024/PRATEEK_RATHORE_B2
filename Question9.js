// Create an array with only positive, and negative numbers, and log its length.
// Create an array with positive and negative numbers
let numbersArray = [1, -2, 3, -4, 5, -6];

// Log the length of the array
console.log("Length of the array:", numbersArray.length);
